services:
  db:
    image: postgres:15
    container_name: fake_job_db_container
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: fake_job_db
    ports:
      - "5432:5432"